{
  "name": "trabecc-cloud",
  "version": "0.2.0",
  "private": true,
  "description": "Trabecc Cloud — multi-host audit retention, dashboards, alerts. Ingest endpoint at api.trabecc.com.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "tsx watch --env-file=.env src/index.ts",
    "start": "tsx src/index.ts",
    "admin": "tsx --env-file=.env src/admin.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "hono": "^4.6.0",
    "postgres": "^3.4.4",
    "stripe": "^17.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.22.0",
    "typescript": "^5.6.0"
  }
}
